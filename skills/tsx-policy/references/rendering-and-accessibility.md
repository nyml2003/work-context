# 渲染与可访问性

## 需要明确的点

- loading、empty、error、disabled 等状态如何渲染
- 焦点、键盘可达和语义标签是否完整
- 条件渲染是否会制造布局跳变或理解断点

## 默认规则

1. 优先使用原生可访问语义，再叠加样式和行为。
2. 状态渲染要成体系，不能只做 happy path。
3. 可点击区域、焦点反馈、禁用状态必须一致可预测。
4. 条件渲染的结构变化要服务理解，不要制造无意义跳闪。

## 输出给 worker 时至少说明

- 关键状态位
- 结构语义要求
- 焦点与交互反馈要求
- 不允许的渲染反模式

