# 传输边界实施

## 核心问题

- 请求从哪里发起
- 原始响应如何映射成 UI 可消费结果
- transport 失败如何转成页面状态

## 默认规则

1. API 调用层和业务消费层之间应有清晰映射。
2. 不把后端字段命名或杂乱结构原封不动传进 UI。
3. view-model 以页面实际需要为准，而不是以响应原样为准。
4. 多个调用协同时，先定义组合结果的状态语义。

## 反模式

- 组件里直接写原始接口响应处理。
- 错误路径没有统一落点。
- 因为接口没定就把 `any` 或宽泛对象一路传下去。

