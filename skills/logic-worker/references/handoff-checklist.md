# 交接清单

## 至少说明

- 状态形态做了哪些改变
- 派生规则和副作用边界有哪些
- 哪些场景已覆盖，哪些仍待补
- UI 或 API 层需要配合什么

## 默认格式

- `summary`: 实现了哪些逻辑
- `decisions`: 状态或副作用上的关键判断
- `artifacts`: hooks、selectors、view-model 或 helper
- `open_questions`: 尚未验证的状态边界

