# 原子类组织

## 关注点

- 布局类、间距类、排版类、颜色类、状态类的组织顺序
- 是否优先使用既有 token
- 响应式类是否表达清晰

## 默认规则

1. class 顺序要稳定，便于扫读和 diff。
2. 优先使用已有 token，不随手引入任意值。
3. 同一层级的 class 应表达一个完整视觉意图，不要散成难读碎片。
4. 响应式类和交互状态类要显式可见，不隐藏在动态拼接里。

## 反模式

- 超长 class string 没有任何组织。
- 大量任意值掩盖真正的设计系统问题。
- 用字符串拼接制造难审查的条件类。

