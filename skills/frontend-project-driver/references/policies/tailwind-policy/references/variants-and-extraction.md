# 变体与抽象边界

## 什么时候该抽离

- 相同视觉模式在多个位置重复出现
- 同一组件存在稳定的状态变体
- 某组 class 已经难以靠注释或分组保持可读

## 默认规则

1. 重复样式优先抽成组件或命名变体，不堆内联 class。
2. 状态组合复杂时，用清晰映射表达，不用多层条件拼接。
3. 不为了“干净”而过早抽象，把一次性样式也包成复杂 helper。
4. 抽象后的名字必须表达视觉或交互意图，而不是技术细节。

## 输出给 worker 时至少说明

- 哪些 class 可以内联
- 哪些样式应抽成变体
- 状态类如何组织
- 哪些抽象会让维护更糟

