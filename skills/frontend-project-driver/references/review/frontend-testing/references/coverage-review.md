# 覆盖评审

## 默认检查项

1. 主要验收标准是否都有对应验证路径。
2. happy path 之外的 loading、empty、error、retry 是否被覆盖。
3. 纯逻辑、组件行为、接口边界分别有没有合适的验证层次。
4. 当前结构是否让关键逻辑可测。

## 评审原则

- 不只看有没有测试文件，要看测试是否覆盖关键用户路径。
- 如果只能靠很重的集成测试覆盖，说明结构可能还不理想。
- 手工验证也可以是验证路径，但必须明确步骤和风险。

