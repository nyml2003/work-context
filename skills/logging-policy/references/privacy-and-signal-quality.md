# 隐私与信号质量

## 隐私边界

- 不记录 token、密码、完整个人敏感信息。
- 不直接落原始请求或原始响应大对象。
- 如果必须记录标识信息，优先使用可控 ID 或脱敏字段。

## 信号质量规则

1. 一条日志必须能回答“为什么要留它”。
2. 频率极高但无筛选价值的日志默认不要。
3. 能由现有错误链路捕获的，不再重复记录同一层噪音。
4. 诊断 breadcrumb 只保留能帮助复盘关键路径的节点。

## 输出给 worker 时至少说明

- 哪些字段禁止记录
- 哪些事件必须打
- 哪些事件只在失败路径记录
- 哪些日志会制造噪音，禁止接入

